name: PR9 Deep Panel Regression Validation

on:
  push:
    branches:
      - fix/pr9-panel-tab-persistence
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - name: Checkout exact audited source
        uses: actions/checkout@v4
        with:
          ref: 258ef5bda2cd2bd3c492c2d8e380527c092bad1a
          persist-credentials: false

      - name: Verify source and toolchain
        shell: bash
        run: |
          set -euo pipefail
          expected="258ef5bda2cd2bd3c492c2d8e380527c092bad1a"
          actual="$(git rev-parse HEAD)"
          echo "expected source: $expected"
          echo "actual source:   $actual"
          test "$actual" = "$expected"
          sw_vers
          swift --version

      - name: Repository checks
        shell: bash
        run: |
          set -euo pipefail
          ./scripts/checks.sh

      - name: Build isolated Hop Dev app
        shell: bash
        run: |
          set -euo pipefail
          ./scripts/build-app.sh --dev
          test -d "dist/Hop Dev.app"

      - name: Package and fingerprint
        shell: bash
        run: |
          set -euo pipefail
          /usr/libexec/PlistBuddy -c "Print :CFBundleIdentifier" "dist/Hop Dev.app/Contents/Info.plist"
          codesign --verify --deep --strict --verbose=2 "dist/Hop Dev.app"
          ditto -c -k --sequesterRsrc --keepParent "dist/Hop Dev.app" "Hop-Dev-PR9-deep-fix-258ef5b.zip"
          shasum -a 256 "Hop-Dev-PR9-deep-fix-258ef5b.zip" | tee "Hop-Dev-PR9-deep-fix-258ef5b.sha256"

      - name: Upload candidate
        uses: actions/upload-artifact@v4
        with:
          name: hop-dev-pr9-deep-fix-258ef5b
          path: |
            Hop-Dev-PR9-deep-fix-258ef5b.zip
            Hop-Dev-PR9-deep-fix-258ef5b.sha256
          retention-days: 7
          if-no-files-found: error
